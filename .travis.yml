language: node_js
node_js:
  - "node"
services:
  - postgresql
before_script:
  - psql -c 'create database project_test;' -U postgres
  - knex migrate:latest --env=test
  - knex seed:run --env=test
deploy:
  provider: heroku
